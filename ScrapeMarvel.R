# Scrape Data from Marvel Universe Wiki

library(rvest)
library(dplyr)

# First we need to scrape the list of characters in the wiki (or pull from API)

# Then we can scrape data from their individual pages